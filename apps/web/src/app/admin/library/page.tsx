import { LibraryPageClient } from "@/features/library/library-page-client";

export const metadata = { title: "Library" };

export default function AdminLibraryPage() {
  return <LibraryPageClient />;
}
